import { PageLoadingSkeleton } from "@/components/page-loading-skeleton";

export default function CustomCommandsLoading() {
  return (
    <PageLoadingSkeleton
      title="Custom Commands & Menus"
      description="Loading commands, triggers, and interactive dropdown menus..."
    />
  );
}
