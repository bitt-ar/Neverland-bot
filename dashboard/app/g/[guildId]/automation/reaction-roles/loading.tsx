import { PageLoadingSkeleton } from "@/components/page-loading-skeleton";

export default function ReactionRolesLoading() {
  return (
    <PageLoadingSkeleton
      title="Reaction Roles"
      description="Loading configured reaction role messages..."
    />
  );
}
