import { PageLoadingSkeleton } from "@/components/page-loading-skeleton";

export default function RadioLoading() {
  return (
    <PageLoadingSkeleton
      title="Radio & 24/7 Broadcast"
      description="Loading broadcast streams, playlists, and audio bots..."
    />
  );
}
