import { PageLoadingSkeleton } from "@/components/page-loading-skeleton";

export default function LevelingLoading() {
  return (
    <PageLoadingSkeleton
      title="Leveling & XP"
      description="Loading XP formulas, rate limits, and level-up rewards..."
    />
  );
}
